<!DOCTYPE html>
<html>
<head>
    <meta charset='utf-8'>
    <title>Lifelog</title>
    <!-- 파비콘 연결 -->
    <link rel="shortcut icon" href="public/img/favicon.png">
    <!-- css 파일 연결 -->
    <link rel="stylesheet" type="text/css" href="public/css/common.css">
    <!-- jquery 연결 -->
    <script src="node_modules/jquery/dist/jquery.js" ></script> 
</head>
<body>
    <div id="header">
        <%- include("./common/header") %>
    </div>
    <div class="wrap">
        <div id="myPage">
            <div id="myPage_top">
                <p> <span><%= name %></span> 님의 마이 페이지</p>
            </div>
            <hr style="margin-top: 20px;">
            <div id="myPage_middle">
                <div id="mp_m_left"><img src="public/img/user_box.png" alt="회원정보 이미지"></div>
                <div id="mp_m_middle"></div>
                <div id="mp_m_right">
                    <form action="/update_db" method="post" style="width: 100%; height:100%;" id="update_form">
                        <ul style="width: 100%; height: 100%;" id="update_form_ul">
                            <li>
                                <div>
                                    <p>Email</p>
                                    <p><%= email %></p>
                                </div>
                            </li>
                            <li>
                                <div>
                                    <p>Password</p>
                                    <input type="password" name="pw" class="chk" title="Password">
                                </div>
                            </li>
                            <li>
                                <div>
                                    <p>Name</p>
                                    <input type="text" placeholder="<%= name%>" name="name" class="chk" title="Name">
                                </div>
                            </li>
                            <li>
                                <div>
                                    <p>Nickname</p>
                                    <input type="text" placeholder="<%= nickname%>" name="nickname" class="chk" title="Nickname">
                                </div>
                            </li>
                        </ul>
                    </form>
                </div>
            </div>
            <hr style="margin-bottom: 20px;">
            <div id="myPage_bottom">
                <a id="update_btn" onclick="submit()">수정하기</a>
                <a href="/mypage" id="cancle_btn">취소하기</a>
            </div>
        </div>
    </div>
    <div id="footer">
        <%- include("./common/footer") %>
    </div>
    <script>
        function emptyCheck(){
            let ok = true;
            $('.chk').each(function(){
                let data = $(this).val().replace(/ /gi, ""); //대소문자 무시하고(i) /사이/의 내용과 완전히 일치할 때(g),
                if(data == ""){
                    alert($(this).attr('title')+'을(를) 공백없이 입력해주세요.')
                    $(this).val('');
                    $(this).focus();
                    ok = false;
                    return ok;
                }
            });
            return ok;
        }

        function submit(){
            if(emptyCheck()){
                if(confirm('이대로 수정하시겠습니까?')) $('#update_form').submit();
            }
        }    
    </script>
</body>
</html>